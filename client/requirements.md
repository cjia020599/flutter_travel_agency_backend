## Packages
@hookform/resolvers | For Zod validation in forms
react-hook-form | Form state management
framer-motion | Smooth transitions and animations
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging tailwind classes safely
lucide-react | For minimalist iconography

## Notes
Clean, minimal aesthetic implemented with Plus Jakarta Sans and Inter fonts.
Forms utilize sticky headers for accessible save actions.
JSONB fields (like includes/excludes) are handled gracefully via line-break separated text areas for excellent minimal UX.
